{
  "schemaVersion": 1,
  "project": "orca-strator",
  "developmentStatus": "IMPLEMENTING",
  "highLevelGoal": "Reopen Orca-Strator V1 as NOT YET QUALIFIED and perform a deep runtime-integration hardening campaign until the real production path works end-to-end: watcher->loop->executor->result->loop->Sol wake wired in production, correct Windows/WSL/Git/browser/recovery/control semantics, and a real qualification tier that proves the pipeline with real Git, real child-process executors, and durable result manifests.",
  "activeMilestone": null,
  "activeChange": "009-v1-runtime-integration-hardening",
  "completedMilestones": [0, 1, 2, 3, 4, 5, 6, 7, 8],
  "planningBaseSha": null,
  "checkpoint": {
    "summary": "Change 009 final correctness pass (items 1-10). Items 1-4: exact Sol wake intent persisted durably - new `sol_operations` table (migration v9) + `SolOperationStore` (SQLite-backed, in-memory fallback); `BrowserManager.rehydrateFromStore` now reads the durable op (no more INITIAL/repoName-from-goal/retryCount-reset guessing); retry counts (timeout/busy) persist so restarts cannot reset the one-time budget; BUSY backpressure made restart-safe (durable busyRetryCount, loop owns retry scheduling, no duplicate unlimited attempts, SOL_STALLED after budget exhausted); strict Sol control correlation mirrors dispatch - `WatcherService` callback carries (repositoryId, controlId, decision, runId, iteration, relatedDispatchId), `LoopService.validateSolControl` validates repositoryId/runId/iteration/relatedDispatchId/status BEFORE any close/consume/mutation, stale controls from older iterations rejected observably (status 'rejected', 'watcher.control_rejected' emitted) and never affect the current turn. Items 5-6: the previously-skipped REAL tests are now non-skipped and PASSING - `real-runtime-controls.test.ts` proves per-repo emergency-kill isolation (kill A terminates only A's process tree -> RECOVERY_REQUIRED, dispatch A not falsely consumed; sibling B stays alive, finishes naturally, result consumed) and wall-clock active-actor ceiling (slow executor active, deadline crosses -> DRAINING, executor NOT killed, finishes naturally -> CEILING_REACHED, no Sol wake). Item 8: Playwright provisioning pinned - `playwright-core` and `playwright` both exact 1.62.1; `browser:install` now `playwright install chromium` (no unversioned `npx --yes`), so installer + runtime + Chromium revision (chromium-1234) align and a clean install is reproducible. Items 7/9: README/ROADMAP/tasks/state reconciled to truthful labels; emergency-kill + wall-clock promoted to real MACHINE-QUALIFIED; Active implementation plan points to Change 009. WSL.1 now genuinely passes after fixing WSL remote-URL handling in WatcherService (ls-remote) and ExecutorService (postflight getRemoteHeadSha) so git runs against the Linux mount path, not the Windows path.",
    "updatedAt": "2026-08-20T13:45:00+08:00",
    "lastCompletedTask": "Items 1-10 of Change 009 final correctness pass complete: persisted exact Sol wake intent durably (sol_operations table + SolOperationStore); BUSY retry durable + restart-safe; strict Sol control correlation with pre-consumption validation (rejected/auditable, no stale control affects current turn); real emergency-kill isolation + wall-clock ceiling tests un-skipped and passing; WSL pipeline fixed and Q.WIN.WSL.1 passing; Playwright pinned to exact 1.62.1; all docs/state reconciled.",
    "lastVerification": "All gates GREEN: `npm test` 173 passed (fast); `npm run test:real` 10/10 passed (Q.WIN.1/2/3, Q.WIN.WSL.1 via REAL wsl.exe now passing after WSL remote-URL fixes in WatcherService + ExecutorService, Q.APP.1, H.4, and real-runtime-controls emergency-kill isolation + wall-clock ceiling non-skipped); `typecheck`/`build`/`lint` clean. Chromium chromium-1234 present via pinned playwright 1.62.1. UNQUALIFIED (honest, no faking): real ChatGPT-authenticated wake, Tailscale phone route, real Kimi/Codex inference burn."
  },
  "nextAction": "All final gates pass (fast 173 + test:real 10/10, typecheck/build/lint clean); commit and push coherent Change 009 checkpoint to main.",
  "blockers": [
    {
      "code": "V1_NOT_YET_QUALIFIED",
      "summary": "V1 remains NOT YET QUALIFIED only for the genuinely external dependencies (real ChatGPT-authenticated wake, Tailscale phone route, real Kimi/Codex inference burn); all internal production wiring is now machine-qualified with real Git / real child-process executors / real WSL / real service wiring.",
      "evidence": "Real tier (test:real) now GREEN: emergency-kill isolation + wall-clock ceiling non-skipped and passing; Q.APP.1 + Q.WIN.1/WSL.1/3 solid; strict Sol control correlation + durable Sol-wake/BUSY-rehydration verified. Remaining UNQUALIFIED are external only (ChatGPT auth, Tailscale, inference burn) - honestly not faked.",
      "nextAction": "When Chromium auth / Tailscale become available, run headed setup smoke + same-origin phone smoke; otherwise leave UNQUALIFIED."
    },
    {
      "code": "EXTERNAL_DEPS_MAY_BE_ABSENT",
      "summary": "Real ChatGPT wake and Tailscale phone route require optional external dependencies absent on this machine; they must stay UNQUALIFIED rather than faked.",
      "evidence": "Chromium present and provisionable via browser:install; Kimi/Codex present; ChatGPT auth and Tailscale absent.",
      "nextAction": "When Chromium auth / Tailscale are available, run headed setup smoke and same-origin phone smoke; otherwise leave UNQUALIFIED."
    }
  ],
  "developmentPolicies": {
    "branch": "main",
    "commitAndPush": true,
    "preserveDirtyWork": true,
    "forcePush": false,
    "significantWorkUsesOpenSpec": true,
    "sessionExitRequiresWaypointUpdate": true
  }
}
