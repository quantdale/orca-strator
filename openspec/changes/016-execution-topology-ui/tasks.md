## 1. Shared preset/view contracts

- [ ] 1.1 Add versioned reusable strategy-preset catalog data with explicit
  strategy/boundedness/packet requirements and no hidden routing.
- [ ] 1.2 Add focused topology read-model helpers/types without duplicating
  controller truth.

## 2. Responsive topology UI

- [ ] 2.1 Implement single-agent Sol/dispatch/executor/result/Sol topology
  cards from campaign detail evidence.
- [ ] 2.2 Implement actual SWARM packet and DAG node/dependency/integration
  cards with typed status, duration, identity, usage, permission, and failure
  evidence.
- [ ] 2.3 Render the preset catalog as explicit non-authoring policy reference
  while preserving repository + goal and SINGLE_AGENT default UX.
- [ ] 2.4 Keep the layout readable at desktop and narrow phone widths without
  introducing graph editing or manual node authoring.

## 3. Verification and documentation

- [ ] 3.1 Add shared/component tests for single, swarm, DAG, partial/failure,
  unknown usage, preset, and responsive/non-authoring states.
- [ ] 3.2 Add deterministic API/read-model assertions where topology fields are
  consumed; preserve real qualification labels.
- [ ] 3.3 Reconcile canonical UI/API/architecture/runtime/data/security/test
  docs, README, ROADMAP, and `.agent/state.json`.
- [ ] 3.4 Run focused UI tests, full fast tests, applicable real tests,
  typecheck, build, lint, validate the OpenSpec, commit/push, and continue to
  final repository reconciliation.
